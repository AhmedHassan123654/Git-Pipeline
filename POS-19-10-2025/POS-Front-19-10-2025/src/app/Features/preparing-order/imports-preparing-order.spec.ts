import { ImportsPreparingOrder } from "./imports-preparing-order";

describe("ImportsPreparingOrder", () => {
  it("should create an instance", () => {
    expect(new ImportsPreparingOrder()).toBeTruthy();
  });
});
