import { OnInit, ElementRef, AfterViewInit } from '@angular/core';
import { ModelService } from '../services/model.service';
import { HelperService } from '../services/helper.service';
import { ControllerService } from '../services/controller.service';
import { BookmarkNode } from '../services/objects';
export declare class BookmarksPanelComponent implements OnInit, AfterViewInit {
    model: ModelService;
    helper: HelperService;
    controller: ControllerService;
    element: ElementRef;
    bookmarksPanel: ElementRef;
    rootName: string;
    constructor(model: ModelService, helper: HelperService, controller: ControllerService);
    ngAfterViewInit(): void;
    ngOnInit(): void;
    getImg1(node: BookmarkNode, i: number): string;
    postAction(node: BookmarkNode): void;
    clearSelected(): void;
    create(): void;
    parseNodes(bookmarks: string[], index: number, nodes: BookmarkNode[]): void;
    parseNode(str: string): BookmarkNode;
    unescape(str: string): string;
    get bottom(): string;
}
