import { OnInit } from '@angular/core';
import { ModelService } from '../services/model.service';
import { Menu } from './menu.service';
import { HelperService } from '../services/helper.service';
import { Item, DateTimeObject } from '../services/objects';
export declare class DatePickerMenuComponent implements OnInit {
    model: ModelService;
    helper: HelperService;
    menu: Menu;
    keyTo: boolean;
    closeOnAction: boolean;
    monthesForDatePickerItems: Item[];
    countLetters: number;
    cols: number[];
    rows: number[];
    constructor(model: ModelService, helper: HelperService);
    ngOnInit(): void;
    getButtonSelected(col: number, row: number): boolean;
    getButtonCaption(col: number, row: number): string;
    getTime(): string;
    getFirstDay(): number;
    get showTime(): boolean;
    get key(): DateTimeObject;
    yearAction(input: any): void;
    timeAction(input: any): void;
    getShortDayName(index: number): string;
    getShortDayColor(index: number): string;
    prevMonthButtonAction(): void;
    nextMonthButtonAction(): void;
}
