import { OnInit, EventEmitter } from '@angular/core';
import { ModelService } from '../services/model.service';
import { Item } from '../services/objects';
import { MenuService } from './menu.service';
export declare class VerticalMenuItemComponent implements OnInit {
    model: ModelService;
    menuService: MenuService;
    styleName: string;
    item: Item;
    imageSizesWidth: number;
    imageSizesHeight: number;
    action: EventEmitter<any>;
    over: boolean;
    private _enabled;
    constructor(model: ModelService, menuService: MenuService);
    ngOnInit(): void;
    get enabled(): boolean;
    set enabled(value: boolean);
    get className(): string;
    mouseover(): void;
    mouseout(): void;
    click(): void;
    get isSmallItem(): boolean;
}
