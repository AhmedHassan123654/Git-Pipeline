import { OnInit } from '@angular/core';
import { Menu } from './menu.service';
import { ModelService } from '../services/model.service';
import { HelperService } from '../services/helper.service';
export declare class DoubleDatePickerMenuComponent implements OnInit {
    model: ModelService;
    helper: HelperService;
    menu: Menu;
    constructor(model: ModelService, helper: HelperService);
    ngOnInit(): void;
    get showTime(): boolean;
    setTimeInterval(firstDate: Date, secondDate: Date): void;
    getFirstDayOfWeek(): 1 | 0;
    getWeekInterval(now: Date): Date[];
    getValuesByDateRangeName(item: string): void;
}
