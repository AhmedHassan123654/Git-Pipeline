import { OnInit, EventEmitter, ElementRef } from '@angular/core';
import { ModelService } from '../services/model.service';
import { HelperService } from '../services/helper.service';
import { AnimationEvent } from '@angular/animations';
import { FormService } from './form.service';
export declare class BaseFormComponent implements OnInit {
    model: ModelService;
    helper: HelperService;
    formService: FormService;
    element: ElementRef;
    changeVisibility: EventEmitter<any>;
    action: EventEmitter<any>;
    level: number;
    caption: string;
    helpUrl: string;
    fontFamily: string;
    color: string;
    fontSize: string;
    containerPadding: string;
    name: string;
    defaultTop: number;
    showCancel: boolean;
    showButtons: boolean;
    showSeparator: boolean;
    display: string;
    constructor(model: ModelService, helper: HelperService, formService: FormService);
    ngOnInit(): void;
    startAnimation(event: AnimationEvent): void;
    doneAnimation(event: AnimationEvent): void;
}
