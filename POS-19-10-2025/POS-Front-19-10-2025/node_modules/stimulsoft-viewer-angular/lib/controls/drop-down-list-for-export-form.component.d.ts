import { OnInit, EventEmitter } from '@angular/core';
import { Item } from '../services/objects';
export declare class DropDownListForExportFormComponent implements OnInit {
    items: Item[];
    width: number;
    key: string;
    margin: string;
    enabled: boolean;
    styleDisplay: string;
    verticalAlign: string;
    action: EventEmitter<any>;
    constructor();
    ngOnInit(): void;
}
