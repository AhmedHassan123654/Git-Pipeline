name: Full POS CI + E2E Regression Test

on:
  push:
    branches: [ main ]

jobs:
  regression-tests:
    runs-on: windows-latest

    env:
      BACKEND_DIR: POS-19-10-2025/POS-BackPublish
      FRONTEND_DIR: POS-19-10-2025/POS-Front-19-10-2025
      BACKEND_URL: http://localhost:5000  # ✅ استخدام المنفذ 5000 الذي يعمل فعلياً
      FRONTEND_URL: http://localhost:4200

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: ${{ env.FRONTEND_DIR }}/package-lock.json

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 17
          cache: maven

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: |
            3.1.x
            6.0.x

      - name: Install Angular CLI globally
        run: |
          npm install -g @angular/cli@21
          ng version

      - name: Start Backend API
        working-directory: ${{ env.BACKEND_DIR }}
        run: |
          echo "Starting backend on Windows..."
          echo "Current directory: $(pwd)"

          # قتل أي عملية dotnet سابقة
          taskkill /f /im dotnet.exe 2>$null
          Start-Sleep -Seconds 2

          # تشغيل الـ Backend بدون تحديد منفذ (سيستخدم 5000)
          echo "Starting backend (will use port 5000)..."
          Start-Process -NoNewWindow -FilePath "dotnet" -ArgumentList "POS.Web.dll" -RedirectStandardOutput "backend.log" -RedirectStandardError "backend_error.log"
          
          echo "Backend process started..."

          # انتظار حتى يكون Backend جاهزاً على المنفذ 5000
          echo "Waiting for backend to start on port 5000..."
          $success = $false
          for ($i = 1; $i -le 30; $i++) {
            # فحص باستخدام netstat
            $listening = netstat -an | findstr ":5000" | findstr "LISTENING"
            if ($listening) {
              echo "✅ Backend is listening on port 5000!"
              $success = $true
              break
            }

            # فحص HTTP
            try {
              $response = Invoke-WebRequest -Uri "http://localhost:5000" -UseBasicParsing -TimeoutSec 2 -ErrorAction SilentlyContinue
              if ($response.StatusCode -eq 200) {
                echo "✅ Backend responded on port 5000!"
                $success = $true
                break
              }
            } catch {
              # تجاهل الأخطاء ومحاولة مرة أخرى
            }

            echo "Waiting for backend... ($i/30)"
            Start-Sleep -Seconds 2
          }

          if ($success) {
            echo "✅ Backend is running successfully on port 5000!"
            echo "Backend logs (first 10 lines):"
            Get-Content "backend.log" -Head 10
          } else {
            echo "❌ Backend failed to start"
            echo "Backend logs:"
            Get-Content "backend.log" -ErrorAction SilentlyContinue
            echo "Backend error logs:"
            Get-Content "backend_error.log" -ErrorAction SilentlyContinue
            exit 1
          }

      - name: Install Frontend Dependencies
        working-directory: ${{ env.FRONTEND_DIR }}
        run: |
          echo "Installing frontend dependencies..."

          # تنظيف أي تثبيت سابق
          Remove-Item -Recurse -Force node_modules -ErrorAction SilentlyContinue
          Remove-Item -Force package-lock.json -ErrorAction SilentlyContinue

          # تثبيت dependencies مع تجاهل أخطاء peer dependencies
          npm install --legacy-peer-deps --no-audit --force
          echo "Dependencies installed successfully"

      - name: Build Angular Application
        working-directory: ${{ env.FRONTEND_DIR }}
        run: |
          echo "Building Angular application..."
          ng build --configuration production --output-path=dist/POS
          echo "Angular build completed successfully"

      - name: Install Serve and Start Frontend
        working-directory: ${{ env.FRONTEND_DIR }}
        run: |
          echo "Installing serve globally..."
          npm install -g serve
          
          echo "Starting frontend server..."
          Start-Process -NoNewWindow -FilePath "npx" -ArgumentList "serve", "-s", "dist/POS", "-l", "4200" -RedirectStandardOutput "frontend.log" -RedirectStandardError "frontend_error.log"
          
          echo "Frontend process started..."
          
          # انتظار Frontend
          for ($i = 1; $i -le 30; $i++) {
            try {
              $response = Invoke-WebRequest -Uri "http://localhost:4200" -UseBasicParsing -ErrorAction SilentlyContinue
              if ($response.StatusCode -eq 200) {
                echo "✅ Frontend is ready!"
                break
              }
            } catch { }
            echo "Waiting for frontend... ($i/30)"
            Start-Sleep -Seconds 2
          }

      - name: Verify Both Services
        run: |
          echo "=== Final Service Verification ==="
          echo "Checking Backend on http://localhost:5000:"
          try {
            $response = Invoke-WebRequest -Uri "http://localhost:5000" -UseBasicParsing
            echo "✅ Backend: HTTP 200 OK"
          } catch {
            echo "❌ Backend: Not accessible"
          }
          
          echo "Checking Frontend on http://localhost:4200:"
          try {
            $response = Invoke-WebRequest -Uri "http://localhost:4200" -UseBasicParsing
            echo "✅ Frontend: HTTP 200 OK"
          } catch {
            echo "❌ Frontend: Not accessible"
          }

      - name: Checkout E2E Project
        run: |
          git clone https://github.com/teamtester60-code/Full-Test-Case.git projectB

      - name: Run E2E Tests
        working-directory: projectB
        env:
          BACKEND_BASE_URL: http://localhost:5000  # ✅ استخدام 5000
          FRONTEND_BASE_URL: http://localhost:4200
        run: |
          echo "Running E2E tests with:"
          echo "Backend: $env:BACKEND_BASE_URL"
          echo "Frontend: $env:FRONTEND_BASE_URL"
          
          mvn clean test -DbrowserType="chrome" -DexecutionType="local" -Dmaven.test.failure.ignore=true

      - name: Upload Test Reports
        uses: actions/upload-artifact@v5
        with:
          name: regression-reports
          path: |
            projectB/test-output/reports/**/*
            projectB/target/surefire-reports/*.*
          retention-days: 30

      - name: Upload Application Logs
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: application-logs
          path: |
            ${{ env.BACKEND_DIR }}/backend.log
            ${{ env.BACKEND_DIR }}/backend_error.log
            ${{ env.FRONTEND_DIR }}/frontend.log
            ${{ env.FRONTEND_DIR }}/frontend_error.log
          retention-days: 30